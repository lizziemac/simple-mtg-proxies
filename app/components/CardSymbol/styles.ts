import styled from 'styled-components';

export const StyledCardSymbol = styled.svg`
  min-width: 3mm;
  height: 3mm;
  vertical-align: middle;
  overflow: visible; /* prevents clipping */
  transform: translateY(-8%);
`;
